"use strict";
let newusers = [];
let current_users = ['Mussa', 'Abdullah', 'Ali'];
if (users.length === 0) {
    console.log('We need to find some users');
}
else {
    let P_age2 = 7;
    if (P_age2 < 2) {
        console.log("You're a baby");
    }
    if (P_age2 >= 2 && P_age2 < 4) {
        console.log("You're a toddler");
    }
    if (P_age2 >= 4 && P_age2 < 13) {
        console.log("You're a kid");
    }
    if (P_age2 >= 13 && P_age2 < 20) {
        console.log("You're a teenager");
    }
    if (P_age2 >= 20 && P_age2 < 65) {
        console.log("You're an adult");
    }
    if (P_age2 > 65) {
        console.log("You're an elder person");
    }
    users.length = 0;
    console.log(current_users, 'The users list is now empty.');
}
